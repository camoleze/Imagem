@model Imagem.Models.exemplo

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
    <script src="~/Scripts/jquery-1.10.2.js"></script>
    <script src="~/Scripts/bootstrap.js"></script>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div class="container-fluid">
        <h4>exemplo</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Titulo)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Titulo)
            </dd>
            <hr />
          <dt>
                @Html.DisplayNameFor(model => model.descricao)
            </dt>

            <dd>
                @Html.DisplayTextFor(model => model.descricao)
            </dd>
            <br />
            <dt>

                <img src="@Url.Content(@Model.caminhoImagem)" alt="@Model.caminhoImagem" width="100" height="100"  />
            </dt>
             <hr />
        </dl>
    
        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
    
            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-success" /> |
                @Html.ActionLink("Voltar", "Index", null, new { @class = "btn btn-primary btn-large" })
            </div>
        }
    </div>
</body>
</html>
